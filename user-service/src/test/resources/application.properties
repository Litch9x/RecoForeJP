# テスト共通設定（DB を必要としないテストは "no-db" profile を有効化）
spring.flyway.enabled=false

# Testcontainers の Postgres は @ServiceConnection で接続するため、
# デフォルトの DB autoconfig は有効のままにする。
# DB を不要にしたい単純なコンテキストテストは
# @ActiveProfiles("no-db") を付与すること。
