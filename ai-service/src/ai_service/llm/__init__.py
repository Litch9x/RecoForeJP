"""LLM 連携モジュール（論文 3.4.3）.

推薦結果に自然な日本語の説明文を生成する。プロバイダ差し替え可能：

- OpenAI (gpt-4o-mini など): ``OPENAI_API_KEY`` 環境変数があれば利用
- フォールバック: 環境変数なし／openai 未インストール時はテンプレベースで日本語化

将来的に意図抽出（自由テキスト → 構造化検索条件）も同じ ``LLMClient`` 抽象で実装する。
"""
